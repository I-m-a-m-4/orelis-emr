{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only permissions. The updater and process plugins do not exist on Android or iOS, where the app stores handle updates — listing these in the shared capability would fail the mobile build.",
  "platforms": ["windows", "macOS", "linux"],
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:default",
    "core:tray:default"
  ]
}
